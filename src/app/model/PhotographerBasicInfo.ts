export default class OLD_PhotographerBasicInfo{
    title:string;
    rating:string;
    orders:number;
    description:string;

  constructor (title:string, rating:string, orders:number, description:string) {
    this.title = title;
    this.rating = rating;
    this.orders = orders;
    this.description = description;
  }
}


